// Moved to the shared @wandit/preview-editor package so the native WebView
// injects the exact same editor script. Shim only — see the package.

export * from "@wandit/preview-editor/editor-script";
