// TODO: replace with the credits API (features/credits/api/) once the ledger
// endpoints are wired; mirrors apps/web features/credits semantics.
export const MOCK_CREDITS = {
	balance: 96,
	/** Beta grant ceiling used by the progress bar (balance / grant). */
	grant: 150,
};
